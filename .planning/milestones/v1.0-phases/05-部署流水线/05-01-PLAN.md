---
phase: 05-部署流水线
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hugo.yaml
  - .github/workflows/hugo.yaml
  - .gitmodules
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
user_setup:
  - service: github
    why: "需要在仓库 Settings 中启用 GitHub Pages 并配置自定义域名"
    manual_steps:
      - task: "进入仓库 Settings → Pages"
      - task: "在 Build and deployment 部分，Source 选择 'Deploy from a branch'"
      - task: "Branch 选择 'gh-pages'，目录选择 '/(root)'"
      - task: "在 Custom domain 输入 gen-code.top，勾选 Enforce HTTPS"
  - service: cloudflare
    why: "配置 DNS 记录指向 GitHub Pages 并启用 HTTPS 代理"
    manual_steps:
      - task: "登录 Cloudflare 控制台，选择 gen-code.top 域名"
      - task: "进入 DNS 设置，添加 CNAME 记录: www -> it00021hot.github.io (Proxied)"
      - task: "添加 A 记录: @ -> 185.199.108.153 (Proxied)"
      - task: "添加 A 记录: @ -> 185.199.109.153 (Proxied)"
      - task: "添加 A 记录: @ -> 185.199.110.153 (Proxied)"
      - task: "添加 A 记录: @ -> 185.199.111.153 (Proxied)"
must_haves:
  truths:
    - GitHub Actions 工作流文件存在于 .github/workflows/
    - 推送代码到 main 分支触发自动构建部署
    - 部署使用 gh-pages 分支作为输出
  artifacts:
    - path: ".github/workflows/hugo.yaml"
      provides: "自动化部署工作流"
      min_lines: 30
    - path: ".gitmodules"
      provides: "PaperMod 主题子模块配置"
      contains: "PaperMod"
    - path: "hugo.yaml"
      provides: "HTTPS baseURL 配置"
      contains: "https://gen-code.top"
  key_links:
    - from: ".github/workflows/hugo.yaml"
      to: "peaceiris/actions-hugo@v3"
      via: "uses"
      pattern: "peaceiris/actions-hugo"
    - from: ".github/workflows/hugo.yaml"
      to: "peaceiris/actions-gh-pages@v3"
      via: "uses"
      pattern: "peaceiris/actions-gh-pages"
    - from: "workflow checkout"
      to: ".gitmodules"
      via: "submodules: true"
---

<objective>
创建 GitHub Actions 自动部署工作流，配置 HTTPS 支持
</objective>

<context>
@/Users/zhihu/projects/claw-blog/hugo.yaml
@/Users/zhihu/projects/claw-blog/.planning/phases/05-部署流水线/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 .gitmodules 配置 PaperMod 主题子模块</name>
  <files>.gitmodules</files>
  <action>
    创建 .gitmodules 文件，配置 PaperMod 主题为 git submodule:
    - url: https://github.com/adityatelange/hugo-PaperMod.git
    - path: themes/PaperMod

    此配置确保 GitHub Actions 可以通过 submodules: true 获取主题文件。
  </action>
  <verify>
    <automated>cat .gitmodules | grep -q "PaperMod"</automated>
  </verify>
  <done>.gitmodules 文件包含 PaperMod 子模块配置</done>
</task>

<task type="auto">
  <name>Task 2: 创建 GitHub Actions 工作流文件</name>
  <files>.github/workflows/hugo.yaml</files>
  <action>
    创建 .github/workflows/hugo.yaml 工作流文件:
    - 触发条件: push 到 main 分支
    - 使用 peaceiris/actions-hugo@v3 安装 Hugo (版本 0.119.0)
    - 使用 peaceiris/actions-gh-pages@v3 部署到 gh-pages 分支
    - 配置 submodules: true 获取 PaperMod 主题
    - 添加 concurrency 配置防止并发部署冲突
    - 构建命令: hugo --minify
  </action>
  <verify>
    <automated>test -f .github/workflows/hugo.yaml && echo "工作流文件已创建"</automated>
  </verify>
  <done>GitHub Actions 工作流文件已创建，配置正确</done>
</task>

<task type="auto">
  <name>Task 3: 更新 hugo.yaml baseURL 为 HTTPS</name>
  <files>hugo.yaml</files>
  <action>
    修改 hugo.yaml 中的 baseURL 配置:
    - 旧值: baseURL: "http://gen-code.top"
    - 新值: baseURL: "https://gen-code.top"

    GitHub Pages 强制 HTTPS 后需要使用 https:// 协议。
  </action>
  <verify>
    <automated>grep 'baseURL: "https://gen-code.top"' hugo.yaml</automated>
  </verify>
  <done>hugo.yaml baseURL 已更新为 https://gen-code.top</done>
</task>

</tasks>

<verification>
- .gitmodules 文件存在且包含正确的 PaperMod 配置
- .github/workflows/hugo.yaml 文件存在且语法正确
- hugo.yaml 中 baseURL 已更新为 https://
</verification>

<success_criteria>
1. GitHub Actions 工作流文件存在于 .github/workflows/
2. 工作流配置包含 submodules: true 以获取 PaperMod 主题
3. baseURL 配置为 HTTPS
</success_criteria>

<output>
After completion, create `.planning/phases/05-部署流水线/05-01-SUMMARY.md`
</output>
