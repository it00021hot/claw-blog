---
phase: 01-项目初始化与主题集成
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hugo.yaml
  - config/_default/
  - content/posts/.gitkeep
  - content/fiction/.gitkeep
  - layouts/.gitkeep
  - static/.gitkeep
  - assets/.gitkeep
  - themes/PaperMod
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - BASE-03
  - THEM-01
  - THEM-02
  - THEM-03

must_haves:
  truths:
    - "运行 hugo server 可以启动本地服务器并访问博客首页"
    - "暗色/亮色主题可以通过切换按钮正常切换"
    - "响应式布局在移动端（375px）正常显示，无横向滚动"
    - "代码块有正确的语法高亮显示"
  artifacts:
    - path: "hugo.yaml"
      provides: "Hugo 站点配置文件"
      contains: "theme: PaperMod"
    - path: "themes/PaperMod"
      provides: "PaperMod 主题（git submodule）"
      contains: ".git"
    - path: "content/posts"
      provides: "技术文章内容目录"
    - path: "content/fiction"
      provides: "短篇小说内容目录"
  key_links:
    - from: "hugo.yaml"
      to: "themes/PaperMod"
      via: "theme: PaperMod 配置"
---

<objective>
创建 Hugo 项目基础结构，集成 PaperMod 主题，配置暗色/亮色主题切换和代码高亮。

Purpose: 为整个博客项目奠定基础设施，确保后续阶段可以基于此构建内容、SEO、广告等功能。

Output:
- Hugo 项目目录结构（content/posts/, content/fiction/, layouts/, static/, assets/）
- PaperMod 主题（通过 git submodule）
- hugo.yaml 基础配置文件
- 可运行的博客预览
</objective>

<context>
@/Users/zhihu/projects/claw-blog/.planning/REQUIREMENTS.md
@/Users/zhihu/projects/claw-blog/.planning/phases/01-项目初始化与主题集成/01-CONTEXT.md
@/Users/zhihu/projects/claw-blog/.planning/phases/01-项目初始化与主题集成/01-RESEARCH.md

项目约束（来自 CONTEXT.md）:
- Hugo Extended 版本 - 支持 SCSS
- 使用 hugo.yaml 配置
- 使用 git submodule 安装 PaperMod
- 暗色/亮色主题使用 PaperMod 原生支持
- 代码高亮使用 Hugo 内置 Chroma，配色: monokai (暗色) / github (亮色)
- Google AdSense ID: ca-pub-8708502478021488 (Phase 4 使用)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 Hugo 项目结构</name>
  <files>
    - content/posts/.gitkeep
    - content/fiction/.gitkeep
    - layouts/.gitkeep
    - static/.gitkeep
    - assets/.gitkeep
  </files>
  <action>
    创建 Hugo 项目所需的基础目录结构:

    1. 创建 content/posts/ 目录（技术文章）
    2. 创建 content/fiction/ 目录（短篇小说）
    3. 创建 layouts/ 目录（自定义模板）
    4. 创建 static/ 目录（静态资源）
    5. 创建 assets/ 目录（资源管道）

    注意：项目根目录应已有 hugo.yaml（如果是从零开始则需要运行 hugo new site）
  </action>
  <verify>
    <automated>ls -la content/ layouts/ static/ assets/</automated>
  </verify>
  <done>所有必需目录已创建</done>
</task>

<task type="auto">
  <name>Task 2: 安装 PaperMod 主题</name>
  <files>
    - themes/PaperMod
  </files>
  <action>
    使用 git submodule 安装 PaperMod 主题:

    1. 如果 themes/PaperMod 不存在，运行:
       git submodule add --depth=1 https://github.com/adityatelange/hugo-PaperMod.git themes/PaperMod

    2. 如果已存在但未初始化，运行:
       git submodule update --init --recursive

    3. 验证主题安装成功:
       ls themes/PaperMod/layouts/
  </action>
  <verify>
    <automated>ls themes/PaperMod/layouts/ | head -5</automated>
  </verify>
  <done>PaperMod 主题已通过 git submodule 安装到 themes/PaperMod</done>
</task>

<task type="auto">
  <name>Task 3: 创建 hugo.yaml 配置并验证构建</name>
  <files>
    - hugo.yaml
  </files>
  <action>
    创建 hugo.yaml 配置文件，包含:

    1. 基础配置:
       - baseURL: "https://example.org/" (后续修改为实际域名)
       - languageCode: "zh-cn"
       - title: "技术博客"
       - theme: ["PaperMod"]

    2. 输出格式:
       - outputs: home [HTML, RSS]

    3. 分类配置:
       - taxonomies: category, tag, series

    4. 主题参数 (params):
       - defaultTheme: auto (跟随系统，支持手动切换)
       - ShowReadingTime: true
       - ShowCodeCopyButtons: true

    5. 代码高亮配置 (markup.highlight):
       - noClasses: false
       - style: monokai

    配置完成后运行 `hugo server` 验证构建成功。
  </action>
  <verify>
    <automated>hugo --quiet</automated>
  </verify>
  <done>
    hugo.yaml 配置已创建，运行 hugo --quiet 无错误输出
    运行 hugo server 可以访问博客首页
  </done>
</task>

</tasks>

<verification>
- [ ] 运行 `hugo --quiet` 无错误，构建成功
- [ ] 主题切换按钮在页面可见
- [ ] 响应式布局在 375px 宽度无横向滚动
- [ ] 代码块有语法高亮样式
</verification>

<success_criteria>
1. 运行 `hugo server` 可以启动本地服务器（默认端口 1313）
2. 访问 http://localhost:1313 可以看到博客首页
3. 暗色/亮色主题切换按钮可用
4. 页面在移动端（375px）正常显示
5. 代码块有正确的语法高亮
</success_criteria>

<output>
完成后创建 .planning/phases/01-项目初始化与主题集成/01-01-SUMMARY.md
</output>
