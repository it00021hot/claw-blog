---
phase: 07-openclaw101
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fetchers/openclaw101_fetcher.py
  - scripts/parsers/typescript_parser.py
  - scripts/formatters/resource_formatter.py
  - scripts/config.py
  - scripts/fetch_openclaw101.py
  - tests/test_openclaw101_fetcher.py
  - tests/test_typescript_parser.py
  - tests/test_resource_formatter.py
autonomous: true
requirements:
  - OCLAW-01
  - OCLAW-02
  - OCLAW-03
  - OCLAW-04
  - OCLAW-05
  - OCLAW-06

must_haves:
  truths:
    - "可以抓取 openclaw101 仓库的 README 并转换为 Hugo Markdown"
    - "可以抓取 openclaw101 仓库的 Issues 并转换为 Hugo Markdown"
    - "可以抓取 resources.ts 数据文件（357 个资源）"
    - "可以解析 TypeScript 数据结构为 Resource 对象列表"
    - "可以将资源转换为 Hugo Markdown 并保存到 content/tech/"
    - "资源可以根据 10 个分类自动映射到 Hugo categories"
  artifacts:
    - path: "scripts/fetchers/openclaw101_fetcher.py"
      provides: "抓取 resources.ts 文件"
      exports: ["OpenClaw101Fetcher"]
    - path: "scripts/parsers/typescript_parser.py"
      provides: "解析 TypeScript 数组为 Resource 对象"
      exports: ["Resource", "parse_resources"]
    - path: "scripts/formatters/resource_formatter.py"
      provides: "资源格式化 + 分类映射"
      exports: ["ResourceFormatter", "CATEGORY_MAP", "save_resource_as_markdown"]
    - path: "scripts/config.py"
      provides: "openclaw101 仓库配置"
      contains: "openclaw101 in github_repos"
    - path: "scripts/fetch_openclaw101.py"
      provides: "主抓取脚本"
      exports: ["main"]
    - path: "tests/test_openclaw101_fetcher.py"
      provides: "单元测试"
    - path: "tests/test_typescript_parser.py"
      provides: "单元测试"
    - path: "tests/test_resource_formatter.py"
      provides: "单元测试"
  key_links:
    - from: "scripts/fetch_openclaw101.py"
      to: "scripts/fetchers/github_fetcher.py"
      via: "import GitHubFetcher"
      pattern: "GitHubFetcher.*fetch_readme.*fetch_issues"
    - from: "scripts/fetch_openclaw101.py"
      to: "scripts/fetchers/openclaw101_fetcher.py"
      via: "import OpenClaw101Fetcher"
      pattern: "OpenClaw101Fetcher.*fetch_resources"
    - from: "scripts/fetch_openclaw101.py"
      to: "scripts/parsers/typescript_parser.py"
      via: "import parse_resources"
      pattern: "parse_resources.*content"
    - from: "scripts/fetch_openclaw101.py"
      to: "scripts/formatters/resource_formatter.py"
      via: "import ResourceFormatter"
      pattern: "ResourceFormatter.*format_resource"
    - from: "scripts/formatters/resource_formatter.py"
      to: "scripts/formatters/openai_formatter.py"
      via: "import save_as_hugo_markdown"
      pattern: "save_as_hugo_markdown.*output_dir"
---

<objective>
抓取 openclaw101 仓库内容（README、Issues、357 个资源）并转换为 Hugo Markdown 博客文章。

目的：复用 Phase 6 通用工具，新增专用 Fetcher、Parser、Formatter 实现特定仓库抓取。
输出：content/tech/openclaw101-*.md 文章文件
</objective>

<execution_context>
@/Users/zhihu/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-/06-CONTEXT.md
@.planning/phases/06-/06-RESEARCH.md
@.planning/phases/07-https-github-com-mengjian-github-openclaw101/07-RESEARCH.md
@scripts/fetchers/github_fetcher.py
@scripts/fetchers/base.py
@scripts/formatters/openai_formatter.py
@scripts/config.py

<interfaces>
<!-- Phase 6 关键接口，复用这些代码 -->

From scripts/fetchers/base.py:
```python
@dataclass
class Article:
    url: str
    title: str
    content: str
    source: str
    created_at: Optional[datetime] = None
```

From scripts/formatters/openai_formatter.py:
```python
def save_as_hugo_markdown(article, front_matter: Dict, output_dir: str) -> str:
    """Save article as Hugo Markdown file."""
    # output_dir: "content/tech/"
    # 返回保存的文件路径
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 OpenClaw101 Fetcher（抓取 resources.ts）</name>
  <files>scripts/fetchers/openclaw101_fetcher.py</files>
  <action>
继承 BaseFetcher，实现 fetch_resources() 方法：
1. 使用 GitHub API /repos/{owner}/{repo}/contents/src/data/resources.ts 获取文件
2. base64 解码返回原始 TypeScript 内容
3. owner 默认 "mengjian-github"，repo 默认 "openclaw101"

关键代码模式（复用 Phase 6 GitHubFetcher）：
```python
import base64
import httpx

class OpenClaw101Fetcher(BaseFetcher):
    def fetch_resources(self, owner="mengjian-github", repo="openclaw101") -> Optional[str]:
        url = f"https://api.github.com/repos/{owner}/{repo}/contents/src/data/resources.ts"
        # GET 请求，base64 解码 content 字段
```
  </action>
  <verify>
    <automated>pytest tests/test_openclaw101_fetcher.py -x -v</automated>
  </verify>
  <done>OpenClaw101Fetcher.fetch_resources() 返回 TypeScript 源码字符串</done>
</task>

<task type="auto">
  <name>Task 2: 创建 TypeScript 解析器</name>
  <files>scripts/parsers/typescript_parser.py</files>
  <action>
实现 parse_resources() 函数，使用正则表达式解析 TypeScript 对象数组：

1. 定义 Resource 数据类，包含字段：
   - title: str
   - desc: str
   - url: str
   - source: str
   - lang: str (默认 "en")
   - category: str (默认 "official")
   - featured: bool (默认 False)
   - tags: List[str] (默认 [])

2. 实现 parse_resources(content: str) -> List[Resource]：
   - 使用正则匹配 TypeScript 对象数组模式
   - 处理单引号/双引号、可选字段
   - 返回 Resource 对象列表

正则模式参考：
```python
pattern = r"\{\s*title:\s*['\"]([^'\"]+)['\"]"
# 匹配: { title: 'xxx', desc: 'xxx', url: 'xxx', ... }
```
  </action>
  <verify>
    <automated>pytest tests/test_typescript_parser.py -x -v</automated>
  </verify>
  <done>parse_resources() 解析 resources.ts 返回 357 个 Resource 对象</done>
</task>

<task type="auto">
  <name>Task 3: 创建资源格式化器（分类映射 + Hugo 转换）</name>
  <files>scripts/formatters/resource_formatter.py</files>
  <action>
实现 ResourceFormatter 类：

1. 分类映射（CATEGORY_MAP）：
   - official → ["官方资源"]
   - getting-started → ["安装", "教程"]
   - channel-integration → ["进阶"]
   - skill-dev → ["进阶"]
   - video → ["教程"]
   - deep-dive → ["源码解析"]
   - tools → ["工具"]
   - cloud-deploy → ["教程"]
   - use-cases → ["教程"]

2. 实现 format_resource(resource: Resource) -> Dict：
   - 映射 category 到 Hugo categories
   - 生成 description（使用 resource.desc）
   - 生成 tags（使用 resource.tags + 智能补充）
   - 返回 front_matter 字典

3. 实现 save_resource_as_markdown(resource, front_matter, output_dir)：
   - 复用 openai_formatter.save_as_hugo_markdown()
   - 文件名: openclaw101-resource-{slug}.md
   - output_dir: "content/tech/"

4. 添加 AI 增强方法（可选，调用 OpenAI）：
   - enhance_description(resource): 使用 AI 丰富描述
  </action>
  <verify>
    <automated>pytest tests/test_resource_formatter.py -x -v</automated>
  </verify>
  <done>ResourceFormatter.format_resource() 返回正确的 Hugo front_matter</done>
</task>

<task type="auto">
  <name>Task 4: 更新配置和创建主脚本</name>
  <files>scripts/config.py, scripts/fetch_openclaw101.py</files>
  <action>
1. 更新 scripts/config.py：
   - 在 github_repos 列表添加 openclaw101 仓库
   - 或创建专门的 openclaw101 配置

2. 创建 scripts/fetch_openclaw101.py 主脚本：
   - 导入 GitHubFetcher 抓取 README 和 Issues（复用 Phase 6）
   - 导入 OpenClaw101Fetcher 抓取 resources.ts
   - 导入 TypeScriptParser 解析资源
   - 导入 ResourceFormatter 格式化并保存

   流程：
   ```
   1. GitHubFetcher.fetch_readme("mengjian-github", "openclaw101")
   2. GitHubFetcher.fetch_issues("mengjian-github", "openclaw101")
   3. OpenClaw101Fetcher.fetch_resources()
   4. parse_resources(ts_content) -> List[Resource]
   5. 对每个 Resource:
      - ResourceFormatter.format_resource(resource)
      - save_resource_as_markdown(resource, front_matter, "content/tech/")
   ```

   支持参数：--dry-run, --verbose, --output-dir
  </action>
  <verify>
    <automated>python scripts/fetch_openclaw101.py --dry-run</automated>
  </verify>
  <done>fetch_openclaw101.py 运行成功，生成 Hugo Markdown 文件</done>
</task>

<task type="auto">
  <name>Task 5: 创建单元测试</name>
  <files>tests/test_openclaw101_fetcher.py, tests/test_typescript_parser.py, tests/test_resource_formatter.py</files>
  <action>
为每个新模块创建单元测试：

1. tests/test_openclaw101_fetcher.py:
   - test_fetch_resources_returns_content: 验证返回 TypeScript 源码
   - test_fetch_resources_handles_error: 验证错误处理

2. tests/test_typescript_parser.py:
   - test_parse_resources_basic: 验证基本解析
   - test_parse_resources_all_fields: 验证所有字段解析
   - test_parse_resources_handles_missing_fields: 验证可选字段处理

3. tests/test_resource_formatter.py:
   - test_category_mapping: 验证分类映射正确
   - test_format_resource_generates_front_matter: 验证 front_matter 生成
   - test_save_resource_as_markdown: 验证文件保存

测试使用 mock 数据，不依赖真实 API。
  </action>
  <verify>
    <automated>pytest tests/test_openclaw101_fetcher.py tests/test_typescript_parser.py tests/test_resource_formatter.py -x -v</automated>
  </verify>
  <done>所有单元测试通过</done>
</task>

</tasks>

<verification>
1. 运行 pytest 测试套件
2. 执行 python scripts/fetch_openclaw101.py --dry-run 验证脚本可运行
3. 检查生成的文件是否符合 Hugo Markdown 格式
</verification>

<success_criteria>
- OpenClaw101Fetcher 可以抓取 resources.ts 文件
- TypeScript 解析器可以解析 357 个 Resource 对象
- 资源格式化器可以正确映射 10 个分类到 Hugo categories
- 主脚本可以成功运行并生成 Hugo Markdown 文件
- 所有单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-openclaw101/07-01-SUMMARY.md`
</output>
